# Optional. If unset, the Vite dev server proxies /api to http://127.0.0.1:3000 (see vite.config.ts).
# The FastAPI app defaults to port 3000 — do not use 8000 unless you run uvicorn with --port 8000
# and update the proxy target to match.
# VITE_API_URL=http://localhost:3000
